import { RequestOptions } from '@angular/http';

export interface ApiConfig {
  basePath: string;
  pid?: string;
  requestInterceptor: (path: string, requestOptions: RequestOptions) => void;
}

export interface Api{{appName}}ConfigMap {
  {{appName}}: ApiConfig;
}
